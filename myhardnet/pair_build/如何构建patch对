单个 RANSAC 内点要求重投影误差约不超过 0.5 px；
最终必须至少有 8 个内点支持同一个几何变换；
内点不足的整对图像直接丢弃，不产生任何训练 patch。

几何扩展点必须同时满足：
条件	当前阈值	作用
投影距离
≤ 0.5 px
B 点必须非常接近预测位置
方向误差
≤ 30°，按 180° 周期
局部脊线方向应一致
尺度比例
0.8～1.2
两侧关键点尺度不能差异过大
边界距离
≥ 16 px
避免 32×32 patch 越界
局部灰度标准差
≥ 5.0
过滤空白或弱纹理区域




## 结论

`pair_build` 不是依靠单一阈值，而是通过：

> **身份约束 → SIFT 外观匹配 → 双向一致性 → RANSAC 几何验证 → 方向/尺度/纹理过滤 → 空间去重 → patch 质量过滤 → QA**

逐层降低误配概率。

因此，它构建的是**经过多重约束的伪真值正样本**，可信度明显高于“同一手指随机裁两个 patch”，但仍不能等同于人工标注的绝对真值。

---

## 1. 只在同一 `finger_id` 内配图

`pair_build/positive_builder.py` 先按：

- `split`
- `finger_id`

对图片分组，然后只在组内进行两两组合。

也就是说，不同 `finger_id` 的图像不会被构造成正样本。这个约束保证了正样本至少来自元数据定义的同一个手指。

但它只相信目录标签。如果原始目录标错，代码无法自行发现。

---

## 2. SIFT/RootSIFT 提供第一层外观一致性

每张图提取 SIFT 特征，当前配置还启用了 RootSIFT：

- SIFT 提供关键点坐标、方向、尺度；
- RootSIFT 改善描述子的 L2 距离匹配稳定性；
- 使用 BFMatcher 做最近邻匹配。

候选点首先必须通过 Lowe ratio test：

```text
best_distance / second_best_distance <= 0.85
```

它的含义是：最佳匹配不仅要近，还要明显优于第二候选，从而过滤指纹重复脊线造成的模糊匹配。

当前配置还启用了：

```yaml
strict_two_way: true
```

因此 A 图中的点认为 B1 最匹配时，还要求 B1 反向也认为该 A 点最匹配。只有双向一致的 seed match 才能进入 RANSAC。

相关逻辑位于 `pair_build/positive_builder.py` 的 `_collect_ratio_matches` 和 `_build_single_image_pair`。

---

## 3. RANSAC 验证整组匹配是否服从同一几何关系

描述子相似并不足够，因为指纹脊线具有较强重复性。因此 seed matches 还要用于估计 A→B 的几何变换。

当前配置是：

```yaml
ransac_model: partial_affine
ransac_reproj_thresh: 0.5
min_seed_matches: 3
min_inliers_per_image_pair: 8
```

实际含义：

- 使用低自由度的部分仿射模型，主要表达旋转、平移和统一尺度变化；
- 单个 RANSAC 内点要求重投影误差约不超过 `0.5 px`；
- 最终必须至少有 8 个内点支持同一个几何变换；
- 内点不足的整对图像直接丢弃，不产生任何训练 patch。

这一步是可信度的主要来源：单个错误匹配可能偶然发生，但至少 8 个点同时服从同一个几何变换的概率更低。

通过 RANSAC 的 seed 内点保存为：

```text
source=sift_inlier
```

这是当前数据中可信度相对最高的一类。

---

## 4. 几何扩展不是盲目扩展

RANSAC 得到几何矩阵 \(T\) 后，代码会把 A 图的其他 SIFT 点投影到 B 图，尝试补充更多对应关系。

扩展点必须同时满足：

| 条件 | 当前阈值 | 作用 |
|---|---:|---|
| 投影距离 | `≤ 0.5 px` | B 点必须非常接近预测位置 |
| 方向误差 | `≤ 30°`，按 180° 周期 | 局部脊线方向应一致 |
| 尺度比例 | `0.8～1.2` | 两侧关键点尺度不能差异过大 |
| 边界距离 | `≥ 16 px` | 避免 32×32 patch 越界 |
| 局部灰度标准差 | `≥ 5.0` | 过滤空白或弱纹理区域 |

这一类保存为：

```text
source=geometry_expanded
```

它不是 RANSAC 原始内点，因此可信度低于 `sift_inlier`。尤其需要注意：扩展样本的 descriptor ratio 只被记录和参与评分，**没有再次作为硬阈值过滤**；它主要依靠几何、方向和尺度保证。

---

## 5. 候选点会综合评分

每个候选都会计算 `stability_score`，综合考虑：

- 是否为 RANSAC seed 内点；
- descriptor 距离；
- 重投影误差；
- 方向误差；
- 尺度一致性；
- SIFT response；
- 局部纹理强度。

其中 RANSAC seed 额外获得 `0.08` 加分。最终优先保留分数高的候选。

但是需要明确：

> `stability_score` 当前只用于排序，没有配置最低分阈值。

因此，如果某个图像对的有效候选不足 16 个，低分但通过硬条件的候选仍可能进入数据集。

---

## 6. 同一图像对内部去重并限制数量

代码会在同一个 `image_pair_id` 内按分数从高到低选择样本。

如果新候选在 A 侧或 B 侧距离已选点小于：

```yaml
dedup_min_distance: 8.0
```

就视为近重复样本并丢弃。

每对图像最终最多保留：

```yaml
max_positive_per_image_pair: 16
```

这样可以避免某些重叠度特别高的图像对贡献大量相似 patch，完全支配训练集。

但去重只发生在**同一个图像对内部**，没有跨图像对的全局物理点去重。

---

## 7. patch 落盘前还有第二轮质量过滤

通过几何验证后，不会直接写入训练 CSV。`pair_build/patch_extractor.py` 还会：

1. 根据每侧 SIFT 方向旋转对齐；
2. 以关键点为中心裁剪 32×32；
3. 检查两侧 patch 质量；
4. 任意一侧不合格，则整对丢弃。

当前条件是：

| 指标 | 条件 |
|---|---:|
| 裁剪框与图像重叠率 | `≥ 0.80` |
| 黑色/空白像素比例 | `≤ 0.10` |
| patch 灰度标准差 | `≥ 5.0` |
| 有效像素比例 | `≥ 0.35` |

因此严重越界、黑边过多、纯色或弱纹理 patch 不会进入最终 `train_pairs.csv`。

---

## 8. 当前历史数据的实际过滤结果

现有 `hardnet_dataset` 产物显示：

- 处理图像对：148,800
- 成功产生正样本的图像对：97,141，约 **65.3%**
- 被跳过的图像对：51,659，约 **34.7%**
  - seed matches 不足：12,717
  - RANSAC 内点不足：37,994
  - 没有有效候选：947
  - RANSAC 失败：1
- 最终正样本：1,251,762 对
  - `sift_inlier`：866,757，约 **69.2%**
  - `geometry_expanded`：385,005，约 **30.8%**

这说明流水线没有为了追求数量让所有图像对通过；约三分之一的图像对被拒绝。

不过 patch 提取阶段显示：

```text
dropped_pair_count = 0
```

即前面保留下来的 1,251,762 对全部通过 patch 质量检查。这主要说明上游 `border_margin=16` 和纹理过滤已经很严格，**不能反过来证明所有 patch 都是正确对应点**。

---

## 9. 当前仍然存在的可信度缺口

### 9.1 没有完整的变换合理性检查

当前只要求 RANSAC 内点数不少于 8，没有硬性检查：

- RANSAC 内点比例；
- 内点是否覆盖足够大的空间范围；
- 内点是否都挤在一小段重复脊线上；
- 变换尺度的全局合理范围；
- 仿射矩阵是否退化。

在指纹这种重复纹理中，少数错误点仍可能形成一个局部自洽的错误变换。

### 9.2 seed 内点没有方向和尺度硬过滤

方向和尺度阈值只对 `geometry_expanded` 强制执行。

`sift_inlier` 虽然会计算方向、尺度分数，但即使方向或尺度异常，也不会直接拒绝，只会降低排序分数。

### 9.3 QA 只是报告，不参与自动验收

`pair_build/qa_tools.py` 会生成统计和预览，但：

- 不会因为 QA 异常自动使流水线失败；
- 当前只读取 CSV 前 10 个匹配做预览；
- 不是随机抽样；
- 不是按 `split/source/finger_id/score` 分层抽样。

现有报告中的 10 张预览全部是 `sift_inlier`，没有实际覆盖占比约 30.8% 的 `geometry_expanded`，所以当前人工抽查覆盖不足。

### 9.4 可能存在身份划分泄漏

代码确实按 `finger_id` 隔离 split，但当前 `finger_id` 包含了 `R0/R1/R2`：

```text
butieping/dhy_R0/Rgd1245
butieping/dhy_R1/Rgd1245
butieping/dhy_R2/Rgd1245
```

当前实际划分中：

- `dhy_R0` 在 test；
- `dhy_R1`、`dhy_R2` 在 train；
- `fys_R0` 在 train；
- `fys_R1`、`fys_R2` 在 val。

如果 `R0/R1/R2` 表示同一个物理手指的不同旋转或采集轮次，那么训练和验证/测试之间已经存在身份泄漏。

这不一定造成单个 patch 对错误，但会让验证指标偏乐观。只有确认 `R0/R1/R2` 真的是不同物理手指，当前 split 才成立。

### 9.5 125 万对并非 125 万个独立物理点

同一张图会参与大量两两组合，同一个关键点也可能在多个 image pair 中反复出现。

当前只有 `image_pair_id` 和局部去重，没有全局 `physical_point_id`。因此数据量很大，但样本之间高度相关，不能把 125 万直接理解为 125 万个独立标注点。

### 9.6 当前配置与历史构建产物不是同一次构建

当前 `pair_build/config.yaml` 指向：

```text
pair_build/tiny_v12
outputs/tiny_v12
```

但现有 `hardnet_dataset` 的索引和 QA 来自旧路径：

```text
pair_build/data
outputs/hardnet_dataset
```

而 summary 中没有保存完整配置快照或配置哈希。因此现有 125 万对是历史构建结果，不能用来证明当前 `tiny_v12` 配置已经完成同样的验证。

---

## 最终判断

可以把当前可信度分成两档：

- **`sift_inlier`：中高可信度**  
  经过 ratio test、双向一致性、RANSAC 和局部质量过滤，适合作为训练伪标签。

- **`geometry_expanded`：中等可信度**  
  几何约束很严格，但不是原始 RANSAC 内点，而且 descriptor ratio 没有硬门槛，应当单独统计和抽查。

所以准确说法是：

> `pair_build` 通过外观、双向匹配、全局几何、局部几何和 patch 质量多层过滤，把误配概率压低；但当前还没有达到“严格真值数据集”的程度。最大的现实风险不是裁剪，而是 `R0/R1/R2` 的身份定义、几何扩展样本以及 QA 抽样覆盖不足。